import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';
//import { arboria } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });

 // export const arboria = arboria();

 //CSS for Arboria Light 
 //font-family: arboria, sans-serif;
 //font-style: normal;
 //font-weight: 300;
 // Arboria Book same but font-weight: 400;